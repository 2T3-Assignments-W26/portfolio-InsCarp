Git Cheat Sheet

To initialize a repo : git -init or smth like that

add a file to staging area: git add then put the name o it or . to put the whole thing

commiting: using commit - m then the message

get status is basically git status

to clone is git clone then the cloud repo url

version is git --version

else basic is git --system for system type

git --gobal for user

git --config for projects

the git editor is VS Code using: code --wait
